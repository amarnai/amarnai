{
  "name": "amarnai",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "test:sorting": "pnpm --filter @amarnai/ai test:sorting",
    "db:generate": "pnpm --filter @amarnai/db generate",
    "db:migrate": "pnpm --filter @amarnai/db migrate",
    "db:seed": "pnpm --filter @amarnai/db seed",
    "db:reset": "pnpm --filter @amarnai/db reset"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "dotenv-cli": "^8.0.0",
    "eslint": "^9.0.0",
    "typescript-eslint": "^8.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.8.0"
  },
  "packageManager": "pnpm@10.10.0",
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "sharp", "prisma", "@prisma/client", "@prisma/engines"]
  }
}
