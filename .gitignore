# Dependencies
node_modules/

# Build outputs
dist/
.next/
out/

# Next.js auto-generated (must not be committed)
next-env.d.ts

# Stray npm lockfiles (project uses pnpm)
package-lock.json

# Turbo
.turbo/

# Env files — allow only .example templates
.env
.env.*
!.env.*.example
!.env.example

# Private keys and credentials
*.pem
*.key
*.p12
*.pfx
*serviceAccountKey.json
*credentials.json

# Local-only dev seed scripts (contain test credentials — never commit)
packages/auth/scripts/

# Editor
.idea/

# OS
.DS_Store

# DOCS
/docs/
tsconfig.tsbuildinfo
*.tsbuildinfo
