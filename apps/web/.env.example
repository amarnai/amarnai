# Internal shared secret used to authenticate web-app requests to the API server.
# Must match the value set in apps/api/.env.
# Generate a strong random value for production: openssl rand -hex 32
INTERNAL_API_SECRET=dev-internal-secret
