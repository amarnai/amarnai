# Chrome extension signing key — never commit. Used only to pin a stable
# extension ID during development (see README).
key.pem

# Build output (also covered by the root .gitignore).
dist/

# Chrome Web Store upload package (produced by `zip -r ../amarnai-extension.zip .`).
*.zip
