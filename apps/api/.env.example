# Internal shared secret used to authenticate requests from the web app to the API.
# Must match the value set in apps/web/.env.
# Generate a strong random value for production: openssl rand -hex 32
INTERNAL_API_SECRET=dev-internal-secret
